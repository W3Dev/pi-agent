name: pi comment agent reusable

on:
  workflow_call:
    inputs:
      pi_allow_push:
        required: false
        default: "false"
        type: string
      pi_cli_version:
        required: false
        default: "0.58.3"
        type: string
      pi_model:
        required: false
        default: "zai/glm-5"
        type: string
    secrets:
      pi_model_json:
        required: true

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  run-agent:
    name: Run pi agent
    if: >-
      github.event.comment != null &&
      (
        contains(github.event.comment.body, '/pi ') ||
        startsWith(github.event.comment.body, '/pi')
      ) &&
      (
        github.event.comment.author_association == 'OWNER' ||
        github.event.comment.author_association == 'MEMBER' ||
        github.event.comment.author_association == 'COLLABORATOR'
      )
    runs-on: ubuntu-latest

    steps:
      - name: Resolve command metadata
        id: meta
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const body = context.payload.comment?.body || ''
            const command = body.replace(/^\/pi\s*/, '').replace(/\r/g, '')
            const eventName = context.eventName
            const actor = context.actor
            const defaultBranch = context.payload.repository.default_branch

            let targetNumber
            let baseRef = defaultBranch
            let headRef = defaultBranch
            let checkoutRef = defaultBranch
            let isPr = false

            if (eventName === 'pull_request_review_comment') {
              const pr = context.payload.pull_request
              targetNumber = pr.number
              baseRef = pr.base.ref
              headRef = pr.head.ref
              checkoutRef = pr.head.ref
              isPr = true
            } else if (context.payload.issue?.pull_request) {
              targetNumber = context.payload.issue.number
              const pr = await github.rest.pulls.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: targetNumber,
              })
              baseRef = pr.data.base.ref
              headRef = pr.data.head.ref
              checkoutRef = pr.data.head.ref
              isPr = true
            } else {
              targetNumber = context.payload.issue.number
            }

            core.setOutput('command', command)
            core.setOutput('actor', actor)
            core.setOutput('event_name', eventName)
            core.setOutput('target_number', String(targetNumber))
            core.setOutput('base_ref', baseRef)
            core.setOutput('head_ref', headRef)
            core.setOutput('checkout_ref', checkoutRef)
            core.setOutput('is_pr', isPr ? 'true' : 'false')

      - name: Checkout target repository
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.meta.outputs.checkout_ref }}
          fetch-depth: 0

      - name: Checkout pi agent source
        uses: actions/checkout@v4
        with:
          repository: W3Dev/pi-agent
          path: .pi-agent-source

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install jq
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y jq

      - name: Cache npm
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: npm-${{ runner.os }}-pi-${{ inputs.pi_cli_version }}
          restore-keys: |
            npm-${{ runner.os }}-pi-

      - name: Install pi CLI
        shell: bash
        run: |
          set -euo pipefail
          npm install -g @mariozechner/pi-coding-agent@${{ inputs.pi_cli_version }}

      - name: Configure pi models
        shell: bash
        env:
          PI_MODEL_JSON: ${{ secrets.pi_model_json }}
        run: |
          set -euo pipefail
          mkdir -p "$HOME/.pi/agent"
          printf '%s' "$PI_MODEL_JSON" > "$HOME/.pi/agent/models.json"

      - name: List pi models
        shell: bash
        run: |
          set -euo pipefail
          pi --list-models

      - name: Build report directory
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .pi-agent

      - name: Collect git context
        shell: bash
        run: |
          set -euo pipefail
          git status --short > .pi-agent/git-status.txt || true
          git branch --show-current > .pi-agent/current-branch.txt || true
          git log --oneline -n 20 > .pi-agent/recent-commits.txt || true
          git diff --stat > .pi-agent/working-tree-diff-stat.txt || true
          find . -maxdepth 3 -type f \
            ! -path './.git/*' \
            ! -path './node_modules/*' \
            ! -path './.venv/*' \
            | sort | head -n 500 > .pi-agent/file-list.txt

          if [[ '${{ steps.meta.outputs.is_pr }}' == 'true' ]]; then
            git fetch origin '${{ steps.meta.outputs.base_ref }}' --depth=1 || true
            git diff --name-only "origin/${{ steps.meta.outputs.base_ref }}...HEAD" > .pi-agent/pr-files.txt || true
            git diff --stat "origin/${{ steps.meta.outputs.base_ref }}...HEAD" > .pi-agent/pr-diff-stat.txt || true
          else
            touch .pi-agent/pr-files.txt .pi-agent/pr-diff-stat.txt
          fi

      - name: Run pi agent
        shell: bash
        env:
          PI_COMMAND: ${{ steps.meta.outputs.command }}
          PI_IS_PR: ${{ steps.meta.outputs.is_pr }}
          PI_BASE_REF: ${{ steps.meta.outputs.base_ref }}
          PI_HEAD_REF: ${{ steps.meta.outputs.head_ref }}
          PI_EVENT_NAME: ${{ steps.meta.outputs.event_name }}
          PI_ACTOR: ${{ steps.meta.outputs.actor }}
          PI_ALLOW_PUSH: ${{ inputs.pi_allow_push }}
          PI_MODEL: ${{ inputs.pi_model }}
        run: |
          bash .pi-agent-source/scripts/pi-agent-runner.sh

      - name: Commit and push changes
        if: steps.meta.outputs.is_pr == 'true' && inputs.pi_allow_push == 'true'
        shell: bash
        run: |
          set -euo pipefail
          if [[ -n "$(git status --porcelain)" ]]; then
            git config user.name "pi-bot"
            git config user.email "pi-bot@users.noreply.github.com"
            git add -A
            git commit -m "pi: apply requested changes from comment"
            git push origin HEAD:${{ steps.meta.outputs.head_ref }}
          fi

      - name: Post final reply
        uses: actions/github-script@v7
        env:
          PI_TARGET_NUMBER: ${{ steps.meta.outputs.target_number }}
          PI_REPORT_PATH: .pi-agent/final-comment.md
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs')
            const path = process.env.PI_REPORT_PATH
            const body = fs.readFileSync(path, 'utf8')
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: Number(process.env.PI_TARGET_NUMBER),
              body,
            })

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pi-agent-artifacts
          path: .pi-agent/
